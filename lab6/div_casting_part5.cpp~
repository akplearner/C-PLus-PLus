#include <iostream>
using namespace std;

int main(){
	int n;
	cout<<"Please enter Intger n that is greated then 666: \n";
	cin>>n;

	

	if(n<=666){
		cout<<"Invalid input\n";
		return 0;	
	}else{//this number is valid
//int a=n%100,b=n%10;//a is last 2 digits, b is last 1 digit.
		if((n%100/10)+(n%10)==13) { //it is evil
			cout<<"Evil"<<endl;
		} else { //not evil
			cout<<"Not Evil"<<endl;
		}
//cout<<a<<", "<<b;
	}
	return 0;
}
